<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 다른 mapper와 중독되지 않도록 네임스페이스 기재 -->
<mapper namespace="statApp">
	<select id="last" resultType="com.humanstar.crocheck.model.statistics.dto.statApplianceVO">
		SELECT 
		max(cpu_total_pct) as cpu_pct, 
		max(mem_use_pct) as mem_pct,
		max(disk_vol1_usage_pct) as disk_vol1_pct,
		date_trunc('min',hhmmss) as hhmmss 
		FROM tb_usage_system_1min 
		WHERE hhmmss >= now() - interval '10 min' 
		GROUP BY date_trunc('min', hhmmss) 
		ORDER BY hhmmss ASC;
	</select>
	<select id="min">
	</select>
	<select id="hour">
	</select>
	<select id="days">
	</select>

</mapper>